import { Router } from 'express'; import { authenticate,requireRole,requireAccountType } from '../middleware/auth.js'; import { aiController,standardsController,testingController,voiceController } from '../controllers/coreControllers.js'; import { domainController } from '../controllers/domainController.js'; import { reportsController } from '../controllers/reportsController.js'; import { validate } from '../middleware/validate.js'; import { aiSchema,idSchema,listStandardsSchema,pinSchema,voiceSchema,makeHandoutSchema,generateReportSchema } from '../validators/domainValidators.js';
export const standardsRoutes=Router();standardsRoutes.get('/',validate(listStandardsSchema),standardsController.list);standardsRoutes.get('/:id',validate(idSchema),standardsController.get);standardsRoutes.post('/:id/save',authenticate,validate(idSchema),standardsController.save);
export const testingRoutes=Router();testingRoutes.use(authenticate, requireAccountType('organization'));testingRoutes.get('/requirements',testingController.requirements);testingRoutes.get('/search',validate(pinSchema),testingController.list);testingRoutes.get('/:id',validate(idSchema),testingController.get);
export const aiRoutes=Router();
aiRoutes.post('/query',authenticate,validate(aiSchema),aiController.ask);
aiRoutes.get('/conversations',authenticate,aiController.listConversations);
aiRoutes.get('/conversations/:id',authenticate,validate(idSchema),aiController.getConversation);
export const voiceRoutes=Router();voiceRoutes.post('/transcribe',authenticate,validate(voiceSchema),voiceController.transcribe);
const orgProtectedDomain=(domain:Parameters<typeof domainController.list>[0])=>{const router=Router();router.use(authenticate, requireAccountType('organization'));router.get('/',domainController.list(domain));router.get('/:id',validate(idSchema),domainController.detail(domain));return router;};
export const certificationRoutes=orgProtectedDomain('certification');
export const handbookRoutes=orgProtectedDomain('handbooks');handbookRoutes.get('/:id/preview',validate(idSchema),domainController.handbookPreview);handbookRoutes.get('/:id/pdf',validate(idSchema),domainController.downloadHandbookPdf);handbookRoutes.post('/make-handout',validate(makeHandoutSchema),domainController.makeHandout);
export const reportsRoutes=orgProtectedDomain('reports');reportsRoutes.get('/:id/pdf',validate(idSchema),domainController.downloadReportPdf);reportsRoutes.post('/generate',validate(generateReportSchema),reportsController.generate); export const organizationRoutes=orgProtectedDomain('organizations'); export const consumerRoutes=orgProtectedDomain('consumer'); export const trendsRoutes=orgProtectedDomain('trends');
export const adminRoutes=Router();adminRoutes.use(authenticate,requireRole('admin'));adminRoutes.get('/',domainController.list('admin'));adminRoutes.get('/:id',validate(idSchema),domainController.detail('admin'));adminRoutes.post('/:action',domainController.adminAction);
