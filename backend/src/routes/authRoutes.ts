import { Router } from 'express'; import { authController } from '../controllers/authController.js'; import { authenticate } from '../middleware/auth.js'; import { validate } from '../middleware/validate.js'; import { loginSchema,profileSchema,registerSchema,resetSchema,updatePasswordSchema } from '../validators/authValidators.js';
export const authRoutes=Router();authRoutes.post('/register',validate(registerSchema),authController.register);authRoutes.post('/login',validate(loginSchema),authController.login);authRoutes.post('/logout',authenticate,authController.logout);authRoutes.post('/forgot-password',validate(resetSchema),authController.reset);authRoutes.post('/update-password',validate(updatePasswordSchema),authController.updatePassword);authRoutes.get('/me',authenticate,authController.me);authRoutes.patch('/me',authenticate,validate(profileSchema),authController.updateProfile);authRoutes.post('/profile/avatar',authenticate,authController.updateAvatar);
