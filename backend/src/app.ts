import cors from 'cors'; import express from 'express'; import helmet from 'helmet'; import { env } from './config/env.js'; import { errorHandler,notFound } from './middleware/errorHandler.js'; import { apiRoutes } from './routes/index.js';
export const app=express();app.disable('x-powered-by');app.use(helmet({crossOriginResourcePolicy:false}));app.use(cors({origin:env.CLIENT_ORIGIN,methods:['GET','POST','PATCH','PUT','DELETE']}));app.use(express.json({limit:'10mb'}));app.use('/api',apiRoutes);app.use('/datasets',(req, res, next) => { if(!req.path.toLowerCase().endsWith('.pdf')) { return res.status(403).json({error: 'Forbidden'}); } next(); },express.static('../Datasets', { dotfiles: 'ignore' }));app.use(notFound);app.use(errorHandler);
